# Auth.js (auth-astro) — вход по email + паролю на /login
AUTH_SECRET=
AUTH_EMAIL=
AUTH_PASSWORD=
# Нужно только для хостингов без встроенного определения (Cloudflare Pages, Netlify).
# На Vercel не требуется.
AUTH_TRUST_HOST=true
